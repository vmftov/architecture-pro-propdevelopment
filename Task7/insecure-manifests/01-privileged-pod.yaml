apiVersion: v1
kind: Pod
metadata:
    name: pod-privileged
    namespace: audit-zone
spec:
    containers:
        - name: nginx
          image: nginx
          securityContext:
              readOnlyRootFilesystem: true
              runAsNonRoot: true
              privileged: true
